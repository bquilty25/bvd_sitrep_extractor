# Environment / secrets
.env
.claude

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
env/

# PDF archive and raw extraction files (large; regenerated from PDFs on demand)
# Exception: manifest.json is tracked (kept in data/raw/ but not committed since data/raw/ is ignored)
pdfs/
data/raw/

# Slide assets — large PNG images stored locally, not committed
assets/

# OS
.DS_Store
Thumbs.db

/.quarto/
**/*.quarto_ipynb
outputs/*.csv
outputs/*.json
.Rproj.user
