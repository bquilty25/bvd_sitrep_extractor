# Environment / secrets
.env
.claude

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
env/

# PDF archive (large binary files — store locally, do not commit)
pdfs/

# Raw extraction JSONs (large; regenerated from PDFs on demand)
data/raw/

# Slide assets — large PNG images stored locally, not committed
assets/

# OS
.DS_Store
Thumbs.db

/.quarto/
**/*.quarto_ipynb
outputs/*.csv
outputs/*.json
.Rproj.user
